default: &default
  dispatchers:
    - polling_interval: 1      # Polls every 1 sec to trigger tasks accurately
      batch_size: 500
  workers:
    - queues: "*"
      threads: 3
      polling_interval: 2

development:
  <<: *default
